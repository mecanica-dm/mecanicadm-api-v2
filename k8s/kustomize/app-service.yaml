apiVersion: v1
kind: Service
metadata:
  name: mecanica-dm-api-service
  namespace: mecanicadm
spec:
  selector:
    app: mecanica-dm-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer